export interface ProductModelRoot {
  products: Product[];
  total: number;
  skip: number;
  limit: number;
}

export interface Product {
  id: number;
  title: string;
  description: string;
  price: number;
  discountPercentage: number;
  rating: number;
  stock: number;
  brand: string;
  favorite?: boolean;
  category: string;
  thumbnail: string;
  images: string[];
}
